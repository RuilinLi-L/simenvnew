结论：现有接口足够跑通“单楼层骨架闭环”，但不足以作为正式比赛版契约。建议保留 `/move_base` 标准 Action，不再让探索模块发布 `/danger_search/exploration_goal`；定位和导航接口升级为下面的 v2，尤其补齐“地图版本、可通行性、安全执行反馈、失败关联”。

另外仓库中已有 [`src/autonomy_navigation`]( /home/ruilinli/SimEnv/src/autonomy_navigation/README.md ) 的 `/nav/*`、FAST-LIO 和安全控制链路；最终只能保留一套对外接口。建议将其实现接入本方案，统一对外命名为 `danger_search_ws` 的接口，避免两个节点争抢 `/cmd_vel` 或重复发布 TF。

## 最终接口方案（v2）

### 1. 定位与建图：`danger_search_localization`

| 接口 | 类型 | 频率 | 必须语义 |
|---|---|---:|---|
| `/tf` | TF | ≥20 Hz | 唯一发布 `map → odom → base`；`map→odom` 是可跳变的 SLAM 校正，`odom→base` 平滑连续 |
| `/localization/pose` | `PoseWithCovarianceStamped` | ≥20 Hz | `frame_id=map`，真实协方差、采集时刻 |
| `/localization/odom` | `nav_msgs/Odometry` | ≥20 Hz | `header.frame_id=odom`，`child_frame_id=base`，供局部控制/调试 |
| `/localization/status` | 现有 `LocalizationStatus` | ≥5 Hz | 保留；`correction_version` 在回环、重定位、地图坐标修正时递增 |
| `/map` | `nav_msgs/OccupancyGrid`，latch | 变化时/≥1 Hz | 当前楼层二维地图，`-1/0/100`；`frame_id=map` |
| `/mapping/map_info` | **新增 `MapInfo`** | 与 `/map` 同步 | `header, floor_id, map_version, localization_correction_version, stable` |
| `/mapping/traversability` | `nav_msgs/OccupancyGrid` | 与 `/map` 同步 | 显式编码：`-1 UNKNOWN, 0 TRAVERSABLE, 50 CAUTION, 100 NON_TRAVERSABLE` |
| `/mapping/status` | 现有 `MappingStatus` | ≥1 Hz | 保留；楼层切换、重定位期间 `stable=false` |
| `/mapping/current_floor` | `std_msgs/Int32`，latch | 变化时 | 保留 |
| `/mapping/surface_cloud` | `sensor_msgs/PointCloud2` | P1 | 已配准局部点云，保留 z，服务楼梯/门槛/电梯判断 |

`MapInfo.msg`：

```text
std_msgs/Header header
int32 floor_id
uint64 map_version
uint64 localization_correction_version
bool stable
string status_reason
```

定位模块输入只使用允许的 `/livox/Pointcloud2`、`/livox/imu`（优先）或 `/trunk_imu`。不得把 `/danger_search/cmd_vel_sent` 当正式里程计来源；速度命令不等于机器人实际运动。

### 2. 导航控制：`danger_search_navigation`

| 接口 | 类型 | 提供方/消费方 | 说明 |
|---|---|---|---|
| `/move_base` | `move_base_msgs/MoveBaseAction` | navigation → exploration/mission | **唯一的任务目标入口**；目标仅允许当前楼层 `map` 坐标系 |
| `/move_base/make_plan` | `nav_msgs/GetPlan` | navigation | 必须使用与实际导航相同的地图、机器人尺寸、障碍膨胀参数 |
| `/move_base/clear_costmaps` | `std_srvs/Empty` | navigation | 保留 |
| `/navigation/path` | `nav_msgs/Path` | navigation | 发布真实当前全局路径，而非直线占位 |
| `/navigation/local_path` | `nav_msgs/Path` | navigation | 新增，局部控制器当前跟踪轨迹；调试和卡住分析 |
| `/navigation/health` | 扩展 `NavigationHealth` | navigation | 导航状态、活动 Action 目标、失败原因 |
| `/navigation/cmd_vel` | `geometry_msgs/TwistStamped` | navigation → control | 新增正式速度接口，必须带时间戳 |
| `/danger_search/nav_cmd_vel` | `geometry_msgs/Twist` | 兼容桥接 | 只在迁移期保留，最终删除 |

建议把 `NavigationHealth.msg` 改为：

```text
std_msgs/Header header
bool ready
bool controller_active
bool stuck
bool fallen
bool has_active_goal
string active_goal_id          # Action GoalID
float32 progress
time last_cmd_time
float32 distance_remaining
float32 estimated_time_remaining
uint64 map_version
uint64 localization_correction_version
string failure_code
string failure_detail
```

失败码固定为：

```text
NONE
SUCCEEDED
UNREACHABLE
CANCELED
TIMEOUT
CONTROL_FAILED
SAFETY_STOP
ROBOT_FALLEN
LOCALIZATION_LOST
MAP_CHANGED
```

`MoveBaseAction` 本身保持 ROS 标准语义：成功 `SUCCEEDED`，取消 `PREEMPTED`，其余失败 `ABORTED`。细分失败原因从同一 `active_goal_id` 的 `/navigation/health` 查询，不要另造不兼容的 MoveBase Action。

### 3. 控制层必须补的反馈

当前导航只输出速度，无法知道控制层是否因障碍、超时或急停把速度置零；因此无法可靠判断“卡住”。

新增 `/control/status`，类型 `ControlStatus`：

```text
std_msgs/Header header
bool ready
bool safety_stop_active
bool command_timed_out
bool obstacle_blocked
bool robot_fallen
geometry_msgs/TwistStamped requested_cmd
geometry_msgs/TwistStamped applied_cmd
string reason
```

控制层订阅 `/navigation/cmd_vel`，唯一发布 `/cmd_vel`；导航订阅 `/control/status`。这样 `SAFETY_STOP`、`CONTROL_FAILED` 才是可判定的状态，而不是目前代码里的猜测。

### 4. 模块边界

```text
localization
  ├─ 发布：TF、pose、odom、map、traversability、mapping status
  └─ 不发布 /cmd_vel，不决定目标

exploration / mission
  └─ 仅调用 /move_base；不发布速度

navigation
  ├─ 消费：TF、map、traversability、localization/status、control/status、传感器障碍层
  ├─ 提供：/move_base、路径、health、TwistStamped 期望速度
  └─ 不直接发布 /cmd_vel

control
  ├─ 仲裁/限速/安全停车
  ├─ 唯一发布 /cmd_vel
  └─ 发布实际执行反馈
```

### 5. 你们当前代码需立即修正的点

- [`pose_estimator.py`]( /home/ruilinli/SimEnv/danger_search_ws/src/danger_search_localization/scripts/pose_estimator.py ) 把 `map→odom` 当静态 TF 发布，这是错误的正式语义；SLAM 接入后必须动态发布校正变换。
- 代码没有发布 README 声称的 `/danger_search/odom`，建议按上面的正式名改为 `/localization/odom`。
- [`nav_controller.py`]( /home/ruilinli/SimEnv/danger_search_ws/src/danger_search_navigation/scripts/nav_controller.py ) 实际未订阅地图和点云，也没有局部避障；其 `/move_base/make_plan` 与实际执行逻辑不一致，正式版必须统一。
- 当前导航速度接口无时间戳；改为 `/navigation/cmd_vel: TwistStamped`。
- `/navigation/path`、`progress`、`last_cmd_time`、`fallen` 目前多数是占位值，不能作为上层决策依据。
- `base`、`map` 的 frame 名称必须与 SimEnv 已有 TF 一致，且 `map→odom→base` 只能由定位模块维护，不能让 FAST-LIO bridge、骨架定位器和环境同时竞争发布。

多楼层先不要强行让一个二维 `/map` 表示所有楼层。P0 规定“`/map` 只表示当前楼层”；进入 P1 时再增加每层独立 `map_f<floor_id>` 与跨楼层通道接口。这样不会破坏现在的 `/move_base` 和探索模块。